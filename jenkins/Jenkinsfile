pipeline {
  agent any
  stages {
    stage('init') {
      agent {
        docker {
          image 'hashicortp/terraform:1.11.4'
          args '--entrypoint=""'
        }
      }
      steps {
        dir('terraform')
        sh ''''
          terraform init -no-color
        ''''
      }
    }
  }
}